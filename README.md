# Ache seu perdido local app

![Alt text](./ss.png?raw=true "Screenshot")

## Usage

Run the scripts:

- `npm install` - installs dependencies for all packages
- `npm start` - starts the dev server for all packages

Applications will be hosted at:

Ionic app
- http://localhost:3000/

Back-end
- http://localhost:3001/